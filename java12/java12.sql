-- 실행
-- 컨드롤 + 엔터
-- 알트 + X

create table joinus(
	id varchar(10) not null primary key,
	pw varchar(10) not null,
	name varchar(10),
	tel varchar(10),
	addr varchar(30),
	stylewant varchar(20)
	)
	
